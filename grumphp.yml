parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phplint: ~
        phpcsfixer2:
            rules:
                single_import_per_statement: false
        phpunit: ~
    ascii:
        failed: ~
        succeeded: ~
